kind: pipeline
type: docker
name: master
platform:
  os: linux
  arch: arm

steps:
- name: test_greeting
  image: fffaraz/qt:static
  trigger:
    branch:
    - master
    event:
    - push
  commands:
  - ls
