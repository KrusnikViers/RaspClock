dist: xenial
language: cpp
os: linux

addons:
  apt:
    packages:
      - qt515base
      - mesa-common-dev
      - libglu1-mesa-dev
    sources:
      - sourceline: 'ppa:beineri/opt-qt-5.15.0-xenial'

compiler:
  - gcc
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
